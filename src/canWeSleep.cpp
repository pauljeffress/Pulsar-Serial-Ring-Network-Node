/*
 * canWeSleep()
 * 
 * Tests if conditions are right for us to initiate going to sleep.
 */

#include "global_vars.h"

bool canWeSleep(){

  bool status = false;    // FALSE until decided to be TRUE

  return status;
}